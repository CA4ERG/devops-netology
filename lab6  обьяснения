#include <stdio.h> // библиотека стандартного ввода и вывода (printf, fopen, fgets, fprintf)
#include <stdlib.h> // библиотека стандартных функций (atoi, malloc, exit и др.)
#include <locale.h> // библиотека для настройки локализации (русский язык)
#include <string.h> // библиотека для работы со строками (strrchr)

int main() { // главная функция программы, с неё начинается выполнение
    
    setlocale(LC_ALL, "Russian"); 
    // включает русскую локализацию
    // LC_ALL — применить ко всем категориям
    // "Russian" — русский язык для вывода текста
    
    FILE *f1; 
    // указатель на первый файл
    // FILE — специальный тип данных для работы с файлами
    
    FILE *f2; 
    // указатель на второй файл
    
    char s[1024]; 
    // массив символов (строка)
    // хранит одну строку текста длиной до 1023 символов + '\0'
    
    f1 = fopen("fio_goda.txt", "rt"); 
    // fopen — открывает файл
    // "fio_goda.txt" — имя файла
    // "rt":
    // r = read (читать)
    // t = text (текстовый режим)
    
    f2 = fopen("ludy_bolshe_1980.txt", "w"); 
    // открываем второй файл для записи
    // "w" = write (запись)
    // если файла нет — создастся
    // если файл есть — содержимое удалится
    
    while (fgets(s, sizeof(s), f1)) { 
    // fgets читает одну строку из файла
    // s — куда записывать строку
    // sizeof(s) — максимальный размер строки
    // f1 — файл, из которого читаем
    // while работает, пока fgets успешно читает строки
        
        char *posledniy = strrchr(s, ' '); 
        // strrchr ищет ПОСЛЕДНИЙ символ ' ' (пробел)
        // s — строка поиска
        // ' ' — что ищем
        // возвращает указатель на найденный пробел
        
        if (s != NULL) { 
        // проверка, что строка существует
        // НО здесь ошибка логики:
        // s никогда не будет NULL
        // правильнее проверять:
        // if (posledniy != NULL)
            
            int got = atoi(posledniy + 1); 
            // atoi превращает строку в число
            // posledniy + 1:
            // сдвиг указателя на 1 символ вправо
            // пропускаем пробел
            // получаем начало года
            
            if (got > 1980 && got <= 3000) { 
            // проверяем:
            // год больше 1980
            // и не слишком большой
                
                fprintf(f2, "%s", s); 
                // fprintf записывает данные в файл
                // f2 — файл назначения
                // "%s" — вывести строку
                // s — сама строка
            }
        }
    }
    
    printf("Строки, к которым относятся люди, родившиеся позднее 1980 года, были успешно записаны в файл 'ludy_bolshe_1980.txt'\n");
    // printf выводит текст в консоль
    // \n — перенос строки
    
    fclose(f1); 
    // fclose закрывает первый файл
    // обязательно закрывать файлы после работы
    
    fclose(f2); 
    // закрываем второй файл
    
    return 0; 
    // завершение программы
    // 0 означает успешное выполнение
}
