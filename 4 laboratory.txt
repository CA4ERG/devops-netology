#include <stdio.h> // библиотека ввода-вывода
#include <stdlib.h> // стандартная библиотека функций общего назначения
#include <math.h> // математическая библиотека (sqrt() pow ())
#include <locale.h> // библиотека локализации (в программе devcppPortable выводит непонятные символы)
#include "pomosh.h" // обращаемя к файлу pomosh.h за функциями периметра и площади

//перенес эти функции в файл Pomosh.h
/**perimetrik (int a, int b, int c) { // функция периметра треугольника

		return a + b + c;
	} 
double ploshadik (int a, int b, int c) { //функция площади треугольника
		
		double poluperimetr = (a + b + c)/2;
		
		
		return sqrt(poluperimetr*(poluperimetr - a)*(poluperimetr - b)*(poluperimetr - c)); //Формула Герона для нахождения площади по трём сторонам
}
**/

int main() {
	setlocale(LC_ALL, "Russian");
	
	int t,y,u; //стороны треугольника t - нижняя сторона
	
	printf("Укажите размеры сторон треугольника:\n");
	scanf("%d%d%d", &t,&y,&u);
	
	if (t+y>u && t+u>y && y+u>t ) {
		
	
	
	printf ("Cтороны треугольника %d %d %d\n", t,y,u);
	int perimetr = perimetrik (t,y,u);
	printf ("Периметр треугольника равен %d\n", perimetr);
	double ploshad = ploshadik (t,y,u);
	printf ("Площадь треугольника равен %8.2lf\n", ploshad);
}
	else {
		printf("По заданным сторонам, треугольник построить невозможно");
	}

	return 0;
}