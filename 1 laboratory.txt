#include <stdio.h> // библиотека ввода-вывода
#include <stdlib.h> // стандартная библиотека функций общего назначения
#include <math.h> // математическая библиотека (sqrt() pow ())

int main ()
{
 
 float a = 2.0; // вводим вещественный элемент
 float b = 7.0;
 float c = 3.0;
 float D;
 float x1,x2;
  
if (a == 0) { // если a = 0 то...
  printf ("Cимвол a не может равнятся 0");
  return 0;}
 
	D = pow(b, 2) - 4 * a * c;
  x1 = (-b + sqrt(D))/(2*a);
  x2 = (-b - sqrt(D))/(2*a);

if (D>0) { // если D>0 то
  printf ("Дискрименант имеет два корня %f и %f",x1,x2);
    
}
	else if (D==0) { // иначе если D=0
      printf ("Дискрименант имеет один корень");
      
    }
         
     else {
       printf ("Дискрименант не имеет корней");
       }
  		return 0;
          }
                          